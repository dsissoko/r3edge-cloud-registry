hazelcast:
    cluster-name: r3edge-cluster-auto-conf
    instance-name: registry-hz-test
    network:
        join:
            multicast:
                enabled: true
            tcp-ip:
                enabled: false